use std::path::Path;

use tauri::AppHandle;

pub struct AppSetting;

impl AppSetting {
    pub fn create_setting_file(setting_file_path: Path) {
        
    }
    pub fn load_setting_file() {
        
    }
}
